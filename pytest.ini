[pytest]
testpaths = tests
pythonpath = src
markers =
    regression_gate: frozen Replay current_rules regression gate (offline only)
    planning_regression: frozen AutoScholarQuery planning regression gate (gold-blind and offline only)
